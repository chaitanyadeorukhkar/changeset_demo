import React from 'react';
import { View, Text } from 'react-native';

const Details: React.FC = () => (
  <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
    <Text>Your app details</Text>
  </View>
);

export default Details;
